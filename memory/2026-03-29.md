# 2026-03-29

## Bootstrap / first conversation

- Mike Rosenthal named the assistant Janet, after the character from *The Good Place*.
- Janet's current vibe should follow the show's Janet: calm, warm, hyper-capable, and lightly playful.
- Mike prefers to be called Mike.
- Mike's default timezone is ET / America/New_York.
- Janet is installed on a 2014 Mac Mini in Mike's Brooklyn apartment (11231).
- Mike splits time between Brooklyn and Ghent, NY.
- Reliability matters because Mike wants Janet to remain useful when he is not physically present.

## Priorities discussed

- Near-term setup goals: Telegram access, and ideally Tailscale, before Mike leaves Brooklyn on Saturday.
- First priority is memory design and practice.
- Mike wants daily memory files plus a master memory file, plus project-specific memory files/workspaces.
- Preferred structure: one main workspace with separate subfolders for each project/business.
- Mike wants memory to be journal-like and rich, while keeping regular response context from ballooning.
- Mike has gotten into the habit of working on one project at a time in other OpenClaw installs.
- A likely workflow: Mike explicitly says when to resume a project, and at the end of a work block asks Janet to commit the most important parts to memory and prepare next steps for later.

## Business / project context

- Mike runs two businesses: Fanaply and Goodz.
- He plans to add business docs into `assets/` subfolders for later review.
- He shared an example architecture from another machine as a reference only; this install should be rebuilt cleanly from scratch with what is actually needed.

## Goodz GitHub / Vercel project status

- Mike wants the Goodz project connected to GitHub and then to Vercel.
- Current workspace is already a git repo, but it currently contains the whole shared OpenClaw workspace, not a dedicated standalone Goodz app repo.
- Attempted GitHub setup hit a blocker because the machine does not have the `gh` CLI installed.
- Options identified: Mike can create the GitHub repo manually, install `gh`, or let Janet use the GitHub API directly from shell with a token.
- Mike pasted a classic GitHub token in chat for temporary use; because it was exposed in chat, it should be rotated/revoked after use.
- We paused before creating/pushing the GitHub repo or connecting anything to Vercel.
- Next time, resume by deciding the repo strategy first, then create or connect the repo, push code, and only after that wire up Vercel.
