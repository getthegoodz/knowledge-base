# Goodz Publish

This folder holds shareable web-ready artifacts and publishing roots for Vercel deployment.

## Current structure

- `site/` — primary Goodz publishing hub
- `overnight-research/` — standalone first published memo

Long term, prefer publishing new docs through `site/` unless a standalone project is more appropriate.
